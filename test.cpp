/* Copyright 2026 Your Name */

#include <iostream>
#include <cstdlib>

// Плохой код с ошибками для линтера

int main() {
    // 1. ОШИБКА: Утечка памяти (нет delete[])
    int** matrix = new int*[5];  // массив указателей
    
    // 2. ОШИБКА: Смешивание стилей (C++ с C-style cast)
    for(int i = 0; i < 5; i++) {
        matrix[i] = (int*)malloc(sizeof(int) * 5);  // C-style cast
    }
    
    // 3. ОШИБКА: Выход за границы массива (индекс 5)
    for(int i = 0; i <= 5; i++) {  // должно быть i < 5
        for(int j = 0; j <= 5; j++) {  // должно быть j < 5
            matrix[i][j] = i * j;  // ПЕРЕПОЛНЕНИЕ СТЕКА при i=5 или j=5
        }
    }
    
    // 4. ОШИБКА: Лишние пробелы в конце строки (тут есть пробелы)    
    // 5. ОШИБКА: Нет пробелов вокруг операторов  
    int x=10;
    int y=20;
    int z=x+y;
    
    // 6. ОШИБКА: Неинициализированная переменная
    int value;
    std::cout << value << std::endl;  // мусорное значение
    
    // 7. ОШИБКА: Пустой цикл (ничего не делает)
    for(int i = 0; i < 10; i++) {
        // пустое тело цикла
    }
    
    // 8. ОШИБКА: Отступ в 2 пробела вместо 4
  std::cout << "Hello" << std::endl;
    
    // 9. ОШИБКА: Использование C-style массива (не рекомендуется)
    int arr[5];
    arr[5] = 100;  // ВЫХОД ЗА ГРАНИЦЫ!
    
    // 10. ОШИБКА: Утечка памяти (не удалили matrix)
    // Нет delete[] matrix и delete[] для каждой строки
    
    return 0;
}

// 11. ОШИБКА: Неиспользуемая функция
void unusedFunction() {
    std::cout << "Я не вызываюсь" << std::endl;
}

// 12. ОШИБКА: Слишком много параметров (7)
void longFunction(int a, int b, int c, int d, int e, int f, int g) {
    std::cout << a << std::endl;
}

// 13. ОШИБКА: Магические числа (5, 10, 100)
class BadClass {
public:
    void process() {
        for(int i = 0; i < 5; i++) {  // магическое число 5
            int temp = i * 10;         // магическое число 10
            if(temp > 100) {           // магическое число 100
                break;
            }
        }
    }
};

// 14. ОШИБКА: Глобальная переменная
int globalVar = 42;

// 15. ОШИБКА: Смешивание табуляции и пробелов
void badStyle() {
	int x = 5;	// табуляция
    int y = 10; // пробелы
  int z = 15; // 2 пробела
}

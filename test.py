import os, sys  # несколько импортов в одной строке

MYCONSTANT = 3.14    # константы должны быть в верхнем регистре, но пробела нет (должно быть   MY_CONSTANT)

def f(a,b,c):  # имя функции слишком короткое, нет пробелов после запятых
    """функция"""  # docstring должен начинаться с большой буквы и заканчиваться точкой
    if a > b and b > c and a - b == 10 or c is not None:  # сложное условие, мало пробелов
            print("Условие выполнено")  # лишний отступ (4 пробела, а не 8)
    else:
        return 42

def long_function_with_many_parameters(param1, param2, param3, param4, param5, param6):  # OK, но внизу будет проблема
    """
    Эта функция содержит строку, превышающую лимит в 79 символов. Вот эта строка как раз очень длинная и явно выходит за пределы.
    """
    total = param1 + param2 + param3 + param4 + param5 + param6
    return total

# пустая строка содержит пробелы (если тут есть пробелы, линтер укажет на это)
    
def process_data(data):
  # отступ в 2 пробела вместо 4
  result = []
  for i in range(len(data)):  # C-style цикл, лучше for item in data:
      result.append(data[i] * 2)
  return result

def calculate(x, y):
    z = x + y  # переменная z используется только для return
    return z

def unused_function():  # функция не используется (если не вызывается в коде)
    pass

# непоследовательный стиль кавычек
name1 = "Alice"
name2 = 'Bob'  # смешиваются двойные и одинарные кавычки

# лишняя точка с запятой в конце строки;
print("Hello World");

# лишние пробелы вокруг скобок
spam( 42 )  # пробелы внутри круглых скобок

# пробел перед двоеточием
if name1 == "Alice" :  # пробел перед двоеточием
    print("Hi")

# отсутствие пробелов вокруг операторов
x = 5*6 + 7/3  # нужны пробелы вокруг * и /
